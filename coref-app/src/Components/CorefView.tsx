import React, {useState} from 'react';
import { useTheme } from '@mui/material/styles';

export default function CorefView() {
    const theme = useTheme();

    return (
        <>
            <p>Here you can see the corefs</p>
        </>
    );
}
